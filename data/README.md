# Local Data

This directory stores the local SQLite database, raw import files, and optional local backups.

- `integrity-dashboard.sqlite` is created locally by the server or ingest tooling when migrations run.
- `imports/` holds source CSV exports before import.
- `backups/` is reserved for local database snapshots.
